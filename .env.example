# Google Cloud & AI Configuration
# Project ID for Google Cloud services - found in your Google Cloud Console
GOOGLE_CLOUD_PROJECT=your-project-id
# Path to your Google Cloud service account JSON credentials file
GOOGLE_APPLICATION_CREDENTIALS=path/to/credentials.json
# API key for Google Gemini AI
# Get this from https://makersuite.google.com/app/apikey
# You'll need to sign in with your Google account to create a key
API_KEY=your-gemini-api-key

# Vector Database Configuration
VECTOR_DB_PATH=./data/vectordb
VECTOR_DB_TYPE=chromadb  # or milvus

# Slack Configuration (if using Slack interface)
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_APP_TOKEN=xapp-your-app-token
SLACK_SIGNING_SECRET=your-signing-secret

# Application Configuration
MAX_CHUNK_SIZE=1000
CONCURRENT_TASKS=4
LOG_LEVEL=INFO
API_PORT=8000

# Security Configuration
# Controls whether authentication is required for the QA system's REST API
# When true, other applications must provide an API key to access this system's endpoints
# Set to false for local development or when the API is not exposed externally
AUTH_REQUIRED=true 
